# Инструкции по деплою документации

Этот документ описывает различные способы деплоя документации Vue Modeler.

## 🚀 Рекомендуемые варианты деплоя

### 1. Vercel (Рекомендуется) ⭐

**Преимущества:**
- ✅ Автоматический деплой при push в Git
- ✅ Бесплатный SSL сертификат
- ✅ CDN по всему миру
- ✅ Preview deployments для PR
- ✅ Очень простой setup

**Шаги:**

1. Зайдите на [vercel.com](https://vercel.com) и войдите через GitHub
2. Нажмите "Add New Project"
3. Импортируйте репозиторий `vue-modeler`
4. Настройки:
   - **Root Directory**: `docs`
   - **Build Command**: `pnpm build`
   - **Output Directory**: `.vitepress/dist`
   - **Install Command**: `pnpm install`
5. Нажмите "Deploy"

Vercel автоматически определит конфигурацию из `vercel.json`.

**Кастомный домен:**
- В настройках проекта → Domains → добавьте свой домен
- Vercel автоматически настроит DNS

---

### 2. Netlify

**Преимущества:**
- ✅ Автоматический деплой при push в Git
- ✅ Бесплатный SSL
- ✅ CDN
- ✅ Preview deployments
- ✅ Формы и функции

**Шаги:**

1. Зайдите на [netlify.com](https://netlify.com) и войдите через GitHub
2. Нажмите "Add new site" → "Import an existing project"
3. Выберите репозиторий `vue-modeler`
4. Настройки:
   - **Base directory**: `docs`
   - **Build command**: `pnpm build`
   - **Publish directory**: `docs/.vitepress/dist`
5. Нажмите "Deploy site"

Netlify автоматически использует `netlify.toml`.

---

### 3. GitHub Pages

**Преимущества:**
- ✅ Полностью бесплатно
- ✅ Интеграция с GitHub
- ✅ Автоматический деплой через GitHub Actions

**Шаги:**

1. В настройках репозитория → Pages:
   - Source: "GitHub Actions"
2. GitHub Actions workflow уже настроен в `.github/workflows/deploy.yml`
3. При push в ветки `main` или `beta` автоматически запустится деплой
4. Документация будет доступна по адресу:
   - `https://<username>.github.io/vue-modeler/` (если base не указан)
   - Или настраиваемый домен

**Важно:** Если используете кастомный домен, раскомментируйте и настройте `base` в `.vitepress/config.ts`:

```ts
base: '/vue-modeler/', // путь к репозиторию
```

---

### 4. Cloudflare Pages

**Преимущества:**
- ✅ Очень быстрый CDN
- ✅ Бесплатный SSL
- ✅ Preview deployments
- ✅ Интеграция с Git

**Шаги:**

1. Зайдите на [dash.cloudflare.com](https://dash.cloudflare.com)
2. Pages → Create a project → Connect to Git
3. Выберите репозиторий `vue-modeler`
4. Настройки:
   - **Framework preset**: None
   - **Build command**: `cd docs && pnpm install && pnpm build`
   - **Build output directory**: `docs/.vitepress/dist`
5. Нажмите "Save and Deploy"

---

## 📊 Настройка аналитики

### Plausible Analytics (Рекомендуется)

**Бесплатно до 10,000 просмотров/месяц**

1. Зарегистрируйтесь на [plausible.io](https://plausible.io)
2. Добавьте свой сайт
3. В `.vitepress/config.ts` раскомментируйте строку:
   ```ts
   ['script', { 'data-domain': 'your-domain.com', src: 'https://plausible.io/js/script.js', defer: true }],
   ```
4. Замените `your-domain.com` на ваш домен

**Особенности:**
- ✅ GDPR compliant (не требует cookie banner)
- ✅ Легковесный (менее 1KB)
- ✅ Приватный (не передает данные третьим лицам)

---

### Umami Analytics

**Полностью бесплатно (self-hosted)**

1. Разверните Umami на своем сервере или используйте [Umami Cloud](https://umami.is)
2. Создайте сайт в Umami и получите `website-id`
3. В `.vitepress/config.ts` раскомментируйте:
   ```ts
   ['script', { 'data-website-id': 'your-website-id', src: 'https://analytics.example.com/script.js', defer: true }],
   ```
4. Замените `your-website-id` и URL на свои

**Особенности:**
- ✅ Полностью бесплатно
- ✅ Open source
- ✅ GDPR compliant
- ✅ Легковесный

---

### Cloudflare Web Analytics

**Бесплатно**

1. В Cloudflare Dashboard → Analytics → Web Analytics
2. Добавьте сайт и получите токен
3. В `.vitepress/config.ts` раскомментируйте:
   ```ts
   ['script', { src: 'https://static.cloudflareinsights.com/beacon.min.js', 'data-cf-beacon': '{"token": "your-token"}', defer: true }],
   ```
4. Замените `your-token` на свой токен

**Особенности:**
- ✅ Бесплатно для всех
- ✅ Не требует cookie banner
- ✅ Быстрая интеграция

---

### GoatCounter

**Бесплатно для небольших сайтов**

1. Зарегистрируйтесь на [goatcounter.com](https://goatcounter.com)
2. Создайте сайт и получите код
3. В `.vitepress/config.ts` раскомментируйте:
   ```ts
   ['script', { 'data-goatcounter': 'https://your-code.goatcounter.com/count', src: 'https://gc.zgo.at/count.js', async: true }],
   ```
4. Замените `your-code` на свой код

**Особенности:**
- ✅ Бесплатно для небольших сайтов
- ✅ Простой и приватный
- ✅ Open source

---

## 🔧 Локальная проверка перед деплоем

```bash
# Установка зависимостей
pnpm install

# Сборка проекта
pnpm build

# Предварительный просмотр
pnpm preview
```

Проверьте, что все работает корректно перед деплоем.

---

## 🌐 Кастомный домен

Для всех платформ процесс похож:

1. **Vercel/Netlify/Cloudflare**: Добавьте домен в настройках проекта
2. **GitHub Pages**: В настройках репозитория → Pages → Custom domain
3. Обновите DNS записи согласно инструкциям платформы
4. Обновите `base` в `.vitepress/config.ts` если необходимо

---

## 🔄 Автоматический деплой

Все платформы поддерживают автоматический деплой при push в Git:

- **Vercel/Netlify/Cloudflare**: Автоматически при подключении репозитория
- **GitHub Pages**: Через GitHub Actions (уже настроено в `.github/workflows/deploy.yml`)

---

## 📝 Рекомендации

1. **Для начала**: Используйте Vercel - самый простой и быстрый вариант
2. **Для аналитики**: Plausible или Cloudflare Web Analytics - простые и бесплатные
3. **Для приватности**: Umami (self-hosted) - полный контроль над данными
4. **Для скорости**: Cloudflare Pages - самый быстрый CDN

---

## ❓ Проблемы?

Если возникли проблемы с деплоем:

1. Проверьте логи сборки на платформе
2. Убедитесь, что `pnpm build` работает локально
3. Проверьте, что все зависимости установлены
4. Убедитесь, что путь к `outputDirectory` правильный
