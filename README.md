# Vue Modeler Documentation

Документация для Vue Modeler - мощной библиотеки для управления состоянием в Vue.js приложениях.

## Технологии

- **VitePress** - генератор статической документации на основе Vite и Vue 3
- **Vue 3** - для компонентов и реактивности
- **TypeScript** - типизация
- **Markdown** - контент документации

## Разработка документации

### Установка зависимостей

```bash
pnpm install
```

### Запуск в режиме разработки

```bash
pnpm dev
```

Документация будет доступна по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
pnpm build
```

### Предварительный просмотр сборки

```bash
pnpm preview
```

## Структура файлов

```
docs/
├── .vitepress/
│   ├── config.ts           # Конфигурация VitePress
│   ├── theme/
│   │   ├── index.ts        # Кастомная тема (расширение DefaultTheme + custom.css)
│   │   └── custom.css      # Стили темы
│   └── components/         # Vue-компоненты (авто-импорт VitePress)
│       └── Tab.vue         # Компонент вкладки
├── index.md                # Главная страница (EN)
├── ru/
│   └── index.md            # Главная страница (RU)
├── introduction/           # Введение
│   ├── index.md
│   ├── installation.md
│   └── getting-started.md
├── guides/                 # Руководства
├── api/                    # Справочник API
├── advanced/               # Продвинутые темы
├── public/                 # Статические файлы
└── package.json
```

## Кастомные компоненты

В `.vitepress/components/` лежат Vue-компоненты, которые VitePress автоматически подключает глобально (достаточно создать файл — регистрация не нужна):

- **`Tab.vue`** — компонент вкладки (проп `label`). Используется в Markdown для табов.

## Вклад в документацию

1. Создайте новую ветку для изменений
2. Внесите изменения в соответствующие Markdown файлы
3. Проверьте локально с помощью `pnpm dev`
4. Создайте pull request с описанием изменений

## Лицензия

Документация распространяется под той же лицензией, что и Vue Modeler.
